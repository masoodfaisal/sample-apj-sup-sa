# Copy this file to .env.local and uncomment the line below
# to use a specific AWS profile for Bedrock calls.
# AWS_PROFILE=your-sso-profile-name
# Optional: override local virtual key TTL (default in docker-compose is 300000 = 5 minutes)
# VIRTUAL_KEY_TTL_MS=300000
